[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    local: tests that require local files such as PX4 .ulg logs and should not run in normal CI